{
  "name": "@agenthub/provider-plugin-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Template for AgentHub provider plugins — wrap any agent CLI as a provider.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agenthub/provider-sdk": "workspace:*"
  }
}
