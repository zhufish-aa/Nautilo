{
  "name": "@agenthub/provider-plugin-trae",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Nautilo provider plugin for Trae (official traecli over ACP, open-source trae-agent CLI as fallback).",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agenthub/provider-sdk": "workspace:*"
  }
}
