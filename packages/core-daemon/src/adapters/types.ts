/**
 * The adapter contract lives in @agenthub/provider-sdk — the public plugin
 * API shared by built-in adapters and third-party provider plugins. This
 * module re-exports it so existing core-daemon imports stay put.
 */
export * from "@agenthub/provider-sdk";
