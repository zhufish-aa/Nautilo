{
  "name": "@agenthub/provider-plugin-opencode",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Nautilo provider plugin for OpenCode (opencode run --format json).",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agenthub/provider-sdk": "workspace:*"
  }
}
